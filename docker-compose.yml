services:
  pet-todos:
    container_name: pet-todos
    build: .
    image: pet-todos:1.0.0
    cpus: 0.25
    mem_limit: 256m
    mem_reservation: 96m
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - pepet-network

networks:
  pepet-network:
    external: true
